# action.yml
name: 'benauca/action-git'
description: 'Clone a repository'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'docker://drone/git:latest'
